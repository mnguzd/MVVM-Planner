<Window x:Class="Planner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Planner"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="600" Width="1000" 
        ResizeMode="NoResize" WindowStyle="None" Background="{StaticResource Silver }">
    <Window.DataContext>
        <local:AppViewModel></local:AppViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="{StaticResource WetAsphalt}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="40"></ColumnDefinition>
                <ColumnDefinition Width="40"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1" Grid.ColumnSpan="3" Background="{StaticResource Clouds }">
                <Grid Background="{StaticResource PomeGranate}" MouseLeftButtonDown="Border_MouseLeftButtonDown" Height="15" VerticalAlignment="Top">
            </Grid>
            </Grid>
            <Button Grid.Column ="3" Style="{StaticResource ButtonStyle}" 
                    Template="{StaticResource ButtonBaseControlTemplate1}" Margin="0 0 10 0 "
                    Command="{Binding ClosingWindowCommand}">
                <Button.Content>
                    <StaticResource ResourceKey="ExitIcon"></StaticResource> 
                </Button.Content>
            </Button>
            <Button Grid.Column="2" Style="{StaticResource ButtonStyle}" 
                    Template="{StaticResource ButtonBaseControlTemplate1}" Margin="0 0 10  0"
                    Command="{Binding MinimizeWindowCommand}">
                <Button.Content>
                    <StaticResource ResourceKey="MinimizeIcon"></StaticResource>
                </Button.Content>
            </Button>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                </Grid.RowDefinitions>
                <ListView x:Name="ListOfFolders" ItemsSource="{Binding SelectedItem.Tasks, ElementName=FoldersListView}" Background="{StaticResource Clouds}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding ToDo}" Foreground="Black" Height="30" Width="40"></Label>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Grid Grid.Row="1" Background="{StaticResource Clouds}">
                    
                </Grid>
            </Grid>
            <Grid Grid.Column="0" Background="{StaticResource WetAsphalt}">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="50"></RowDefinition>
                </Grid.RowDefinitions>
                <Button Grid.Row="1" Height="50" Width="200" VerticalAlignment="Bottom" Style="{StaticResource ButtonStyle2}" 
                        Template="{StaticResource ButtonBaseControlTemplate1}" Command="{Binding AddFolderCommand}">
                    <Button.Content >
                        <StaticResource ResourceKey="AddIcon"></StaticResource>
                    </Button.Content>
                </Button>
                <Grid Grid.Row="0">
                    <ListView x:Name="FoldersListView" ItemsSource="{Binding Folders}" SelectionMode="Single" 
                                BorderBrush="Transparent" BorderThickness="0" Background="Transparent" Margin="-1 -1 -1 -1" 
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Control Template="{StaticResource MainData}"></Control>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
